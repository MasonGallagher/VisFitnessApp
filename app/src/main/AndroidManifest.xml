<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mason.visfitness">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_inverted"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_inverted"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Activities.MainActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Activities.LandingPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:permission="android.permission.READ_EXTERNAL_STORAGE"
            android:authorities="com.example.mason.visfitness.utils.MyProvider"
            android:multiprocess="true"
            android:exported="true"
            android:name="com.example.mason.visfitness.utils.MyProvider">
        </provider>
    </application>

</manifest>